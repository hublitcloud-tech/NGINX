# Server block específico para o domínio
server {
    listen 80;
    server_name gerenciador-websites.hublit.cloud;

    resolver 127.0.0.11 valid=30s;
    resolver_timeout 5s;

    proxy_connect_timeout 10s;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;

    # Proxy para a API
    location /api {
        set $upstream http://gerenciador_websites_api:3333;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }

    # Proxy para o frontend
    location / {
        set $upstream http://gerenciador_websites_web:80;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Server block padrão (catch-all) para IP e outros hostnames
server {
    listen 80 default_server;
    server_name _;

    resolver 127.0.0.11 valid=30s;
    resolver_timeout 5s;

    # Proxy para a API
    location /api {
        set $upstream http://gerenciador_websites_api:3333;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }

    # Proxy para o frontend
    location / {
        set $upstream http://gerenciador_websites_web:80;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}